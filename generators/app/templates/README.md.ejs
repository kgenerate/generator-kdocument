# <%= app.name %>

![SDLC](https://img.shields.io/badge/SDLC-<%= app.lifeCycle %>-informational)
![Date](https://img.shields.io/badge/BeginDate-<%= new Date(app.beginDate).toLocaleDateString() %>-success)

<%= app.description %>

---

## Dependencies

1. `docker`

## Building

```sh
docker run --rm -it -v "$(pwd):/docs" squidfunk/mkdocs-material build
```

## Starting

```sh
docker run --rm -it -p 8000:8000 -v "$(pwd):/docs" squidfunk/mkdocs-material
```

---

## Links

<%_ (locals.links || []).forEach((link) => { _%>
-   [<%= link.label %>](<%= link.url %>)
<%_ }); _%>

## License

This project is licensed under the [MIT license](LICENSE.md).  
Copyright (c) <%= app.ownerName %> (<%= app.ownerEmail %>)
